name: "Run Gatling (Java-Gradle)"
description: "Runs Gatling tests for the Gradle project"

inputs:
  simulation:
    required: true
  test_duration:
    required: true
  ramp_up_duration:
    required: true
  secret_id:
    required: false
  secret_key:
    required: false

runs:
  using: "composite"
  steps:

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: "17"

    - name: Run Gatling Tests
      shell: bash
      run: |
        ./java-gradle/actions/run-gatling.sh \
        ${{ inputs.simulation }} \
        ${{ inputs.test_duration }} \
        ${{ inputs.ramp_up_duration }} \
        ${{ github.workspace }} \
        ${{ inputs.secret_id }} \
        ${{ inputs.secret_key }}

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: gatling-reports-java-gradle-${{ github.run_number }}
        path: |
          ${{ github.workspace }}/java-gradle/build/reports/gatling/**
        if-no-files-found: warn